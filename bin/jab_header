#!/bin/sh

if [ -z "$1" ]; then
    echo "Usage: $0 <file path>"
    exit 1
fi

HEADERNAME=`basename $1 | tr "[:lower:]" "[:upper:]" | sed -e 's/\./_/g'`

cat << EOF
#ifndef __${HEADERNAME}__
#define __${HEADERNAME}__

#endif /* __${HEADERNAME}__ */
EOF
